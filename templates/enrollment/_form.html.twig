{{ form_start(form) }}

    <h4>Inscription</h4>

    <div class="card p-3 mb-3">
        <h5>Enfant</h5>
        {{ form_row(form.child) }}
    </div>

    <div class="card p-3 mb-3">
        <h5>Année scolaire</h5>
        {{ form_row(form.season) }}
    </div>

    <div class="card p-3 mb-3">
        <h5>Notes</h5>
        {{ form_row(form.notes) }}
    </div>

    <button class="btn btn-primary w-100">
        Valider
    </button>

{{ form_end(form) }}
