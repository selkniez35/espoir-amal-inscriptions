{% extends 'base.html.twig' %}

{% block title %}Enrollment{% endblock %}

{% block body %}
    <h1>Enrollment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ enrollment.id }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ enrollment.status.value }}</td>
            </tr>
            <tr>
                <th>CreateAt</th>
                <td>{{ enrollment.createAt ? enrollment.createAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Notes</th>
                <td>{{ enrollment.notes }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_enrollment_index') }}">back to list</a>

    <a href="{{ path('app_enrollment_edit', {'id': enrollment.id}) }}">edit</a>

    {{ include('enrollment/_delete_form.html.twig') }}
{% endblock %}
